package sc.pirate.app.theme

import androidx.compose.ui.graphics.Color

// Extra colors that exist in the RN theme but don't map cleanly onto Material colorScheme slots.
object PiratePalette {
  val TextMuted = Color(0xFFB7B5A9)
}
